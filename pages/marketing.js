import SnowflakeApp from '../components/SnowflakeApp'
import * as constants from '../constants_marketing';

function Home() {
  return (
    <div>
      <SnowflakeApp constants={constants} />
    </div>
  );
}

export default Home;
